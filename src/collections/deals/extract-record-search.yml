id:
  $var: $.id
name:
  $var: $.title
url:
  $concat: ['https://app.pipedrive.com/deal/', $var: $.id]
fields:
  id:
    $var: $.id
  name:
    $var: $.title
  value:
    $var: $.value
  currency:
    $var: $.currency
  stage_id:
    $var: $.stage.id
  status:
    $var: $.status
  user_id:
    $var: $.owner.id
  org_id:
    $var: $.organization.id
  person_id:
    $var: $.person.id
  visible_to:
    $var: $.visible_to
